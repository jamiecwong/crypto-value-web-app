apiVersion: v1
kind: Service
metadata:
  name: {{ include "cryptoapp.fullname" . }}
  labels:
    {{- include "cryptoapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cryptoapp.selectorLabels" . | nindent 4 }}
