apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cryptoapp.fullname" . }}-test-connection"
  labels:
    {{- include "cryptoapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cryptoapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
